package io.github.alelk.tgvd.domain

/**
 * Domain module entry point.
 *
 * This module contains:
 * - Domain models (sealed classes, data classes, value classes)
 * - Domain services (RuleMatchingService, MetadataResolver)
 * - Use cases (PreviewUseCase, CreateJobUseCase)
 * - Domain errors (sealed interface DomainError)
 * - Ports (repository interfaces)
 *
 * No dependencies on Ktor, DB, or serialization frameworks.
 */

